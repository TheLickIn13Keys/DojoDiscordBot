"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const MessageVisual_1 = require("../../visuals/MessageVisual");
describe('Unit::visuals/MessageVisual', () => {
    describe('constructor', () => {
        it('works', () => {
            const text = 'qa3et46ry3h';
            const options = {
                a: 'b'
            };
            const visual = new MessageVisual_1.MessageVisual(text, options);
            expect(visual.text).toEqual(text);
            expect(visual.options).toEqual(options);
        });
        it('allows optional options', () => {
            const text = 'qa3et46ry3h';
            const visual = new MessageVisual_1.MessageVisual(text);
            expect(visual.text).toEqual(text);
            expect(visual.options).toEqual(undefined);
        });
    });
});
