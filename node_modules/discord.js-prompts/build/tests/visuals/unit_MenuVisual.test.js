"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const MenuVisual_1 = require("../../visuals/MenuVisual");
const MenuEmbed_1 = require("../../MenuEmbed");
describe('Unit::visuals/MenuVisual', () => {
    describe('constructor', () => {
        it('works', () => {
            const menu = new MenuEmbed_1.MenuEmbed();
            const options = {
                a: 'b'
            };
            const visual = new MenuVisual_1.MenuVisual(menu, options);
            expect(visual.text).toEqual('');
            expect(visual.menu).toEqual(menu);
            expect(visual.options).toEqual(options);
        });
        it('allows optional options', () => {
            const menu = new MenuEmbed_1.MenuEmbed();
            const visual = new MenuVisual_1.MenuVisual(menu);
            expect(visual.text).toEqual('');
            expect(visual.menu).toEqual(menu);
            expect(visual.options).toEqual(undefined);
        });
    });
});
